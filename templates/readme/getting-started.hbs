## Getting started

1. Install dependencies using your package manager.
2. Copy `.env.example` to `.env` and fill in values.
3. Run the `dev` script from the appropriate `package.json` (root, client, or server).
