module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
{{#if hasExpoRouter}}
    plugins: ['expo-router/babel'],
{{else}}
    plugins: [],
{{/if}}
  };
};
