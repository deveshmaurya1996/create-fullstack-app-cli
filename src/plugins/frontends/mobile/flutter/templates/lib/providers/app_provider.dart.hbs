import 'package:flutter/foundation.dart';

/// App-wide UI state (extend as needed).
class AppProvider extends ChangeNotifier {
  bool _busy = false;
  bool get busy => _busy;

  void setBusy(bool value) {
    if (_busy == value) {
      return;
    }
    _busy = value;
    notifyListeners();
  }
}
