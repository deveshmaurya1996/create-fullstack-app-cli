import type { LinkingOptions } from '@react-navigation/native';
import type { RootStackParamList } from './types';

export const linking: LinkingOptions<RootStackParamList> = {
  prefixes: ['{{kebabCase projectName}}://'],
  config: {
    screens: {
      MainTabs: 'tabs',
{{#if hasAuth}}
      Login: 'login',
      Register: 'register',
{{/if}}
    },
  },
};
