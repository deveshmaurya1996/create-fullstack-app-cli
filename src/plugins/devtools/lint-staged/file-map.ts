import type { PluginFileMap } from '../../../shared/types.js';

const fileMap: PluginFileMap = {
  files: [{ template: '.lintstagedrc.hbs', outputPath: '.lintstagedrc', target: 'root' }],
  injections: [],
};

export default fileMap;
