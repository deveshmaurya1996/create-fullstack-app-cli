import type { PluginFileMap } from '../../../shared/types.js';

const fileMap: PluginFileMap = {
  files: [{ template: 'hooks/useLocation.ts.hbs', outputPath: 'src/hooks/useLocation.ts', target: 'frontend' }],
  injections: [],
};

export default fileMap;
