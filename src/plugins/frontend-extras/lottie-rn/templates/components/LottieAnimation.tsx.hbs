import LottieView from 'lottie-react-native';

export function LottieAnimation(): JSX.Element {
  return (
    <LottieView
      source={require('../assets/sample-animation.json')}
      autoPlay
      loop
      style={{ width: 200, height: 200 }}
    />
  );
}
