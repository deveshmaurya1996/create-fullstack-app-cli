import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: '{{#if hasPostgres}}postgresql{{else if hasMysql}}mysql{{else}}sqlite{{/if}}',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
