import type { NextFunction, Request, Response } from 'express';

export function luciaAuthMiddleware(req: Request, _res: Response, next: NextFunction): void {
  // Attach session/user from cookies here.
  (req as Request & { user?: unknown }).user = null;
  next();
}
